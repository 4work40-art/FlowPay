-- Заготовка под будущую (пока не реализованную) интеграцию с ЭДО
-- (СБИС/Диадок, опциональный платный модуль — см. lib/edoProvider.js).
-- Поля не используются до реальной интеграции: приложение их нигде не
-- читает и не пишет. Добавлены заранее, потому что это дёшево и безопасно
-- (ADD COLUMN IF NOT EXISTS, NULL по умолчанию, без бэкфилла и блокировок
-- на больших таблицах) — чтобы будущее подключение провайдера не требовало
-- отдельной миграции ядра.

ALTER TABLE outgoing_invoices
  ADD COLUMN IF NOT EXISTS edo_document_id VARCHAR(255),
  ADD COLUMN IF NOT EXISTS edo_status VARCHAR(30);
