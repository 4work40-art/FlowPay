import Link from 'next/link';

export default function TrustPage() {
  return (
    <div style={{ maxWidth: 720, margin: '40px auto', padding: '0 20px', lineHeight: 1.6, color: 'var(--color-text)' }}>
      <h1 style={{ fontSize: 22, marginBottom: 8 }}>Безопасность и защита данных</h1>
      <p style={{ color: 'var(--color-text-secondary)', marginBottom: 24 }}>
        Ниже — как устроена защита данных клиентов в «Счёт&amp;Контроль» на практике,
        без общих обещаний. Это описание принципов работы сервиса, а не сертификат
        и не заключение независимого аудитора.
      </p>

      <h2 style={{ fontSize: 16, marginTop: 24, marginBottom: 8 }}>Изоляция данных между организациями</h2>
      <p>Сервис — мультитенантный: в одной системе работают разные организации-клиенты,
      и данные каждой из них должны быть видны только ей. Принадлежность организации
      определяется на сервере из данных сессии пользователя (после входа в систему),
      а не из параметров, присланных браузером, — клиент не может «представиться» другой
      организацией, изменив запрос. Каждое обращение к счетам, контрагентам и другим
      данным сервер дополнительно сверяет с организацией, к которой относится
      конкретная запись, и при несовпадении возвращает ответ «не найдено», а не чужие
      данные.</p>

      <h2 style={{ fontSize: 16, marginTop: 24, marginBottom: 8 }}>Доступ и аутентификация</h2>
      <p>Вход в систему защищён ограничением числа попыток по IP и по паре
      IP+email — это затрудняет автоматический подбор пароля. Доступ к разделам
      внутри организации разграничен по ролям (владелец, бухгалтер, администратор
      поставщика, только для чтения) — например, списание задолженности или
      разрешение спора с контрагентом доступны только владельцу или бухгалтеру.</p>
      <p>Доступ к административной панели платформы отделён от ролей внутри
      организации-клиента: это отдельный, явно выдаваемый признак учётной записи,
      проверяемый на сервере отдельной проверкой на каждом запросе, а не часть
      обычных ролей клиента. Это значит, что сотрудники, обслуживающие других
      клиентов, не видят ваши данные — а администратор платформы, у которого
      специально включено это право, видит только служебную информацию уровня
      организации (список пользователей, счётчики, журнал действий), но не
      содержимое ваших счетов и карточек контрагентов через обычные функции
      платформы.</p>

      <h2 style={{ fontSize: 16, marginTop: 24, marginBottom: 8 }}>Хранение данных</h2>
      <p>Пароли не хранятся в открытом виде и не восстанавливаются — сохраняется
      только их хеш, полученный алгоритмом bcrypt. Проверка пароля при входе
      сравнивает хеши, а не сами пароли. При смене или сбросе пароля все ранее
      выданные сессии пользователя принудительно завершаются — устройство,
      на котором мог остаться старый доступ (например, при утере пароля), теряет
      его сразу после сброса.</p>
      <p>Ссылка «поделиться счётом», которую можно выдать контрагенту, отключается
      организацией в любой момент — после отключения переход по ней ничего не
      показывает.</p>

      <h2 style={{ fontSize: 16, marginTop: 24, marginBottom: 8 }}>Журналирование и аудит</h2>
      <p>Значимые действия в системе — вход в аккаунт, смена пароля, создание и
      изменение статуса счетов, изменения карточек контрагентов, регистрация
      организации и другие — записываются в журнал аудита с указанием, кто и
      когда это сделал. Журнал используется для расследования нештатных ситуаций
      и не является публичным разделом.</p>

      <h2 style={{ fontSize: 16, marginTop: 24, marginBottom: 8 }}>Персональные данные и 152-ФЗ</h2>
      <p>Регистрация в сервисе требует явного согласия на обработку персональных
      данных — без него аккаунт не создаётся. Состав обрабатываемых данных, цели
      обработки, места хранения и права пользователя на уточнение, блокирование
      или удаление своих данных подробно описаны в{' '}
      <Link href="/privacy">Политике конфиденциальности</Link>. Условия
      использования сервиса — в{' '}
      <Link href="/offer">публичной оферте</Link>.</p>

      <p style={{ marginTop: 24, color: 'var(--color-text-secondary)', fontSize: 13 }}>
        Эта страница описывает текущую реализацию сервиса и не является
        юридическим заверением о соответствии конкретным стандартам, результатом
        независимого аудита безопасности или гарантией отсутствия инцидентов.
        По вопросам безопасности — раздел «Поддержка» личного кабинета.
      </p>
    </div>
  );
}
